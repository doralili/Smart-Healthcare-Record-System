# Smart Healthcare Record System 测试手册

这份手册用于最终上传 GitHub 前的本机验收测试。按顺序执行，全部通过后再提交/上传。

## 0. 测试前准备

### 0.1 确认数据库容器运行

在项目根目录执行：

```powershell
docker ps
```

应看到：

```text
healthcare-opengauss-dev   0.0.0.0:5433->5432/tcp
```

如果没有运行：

```powershell
docker start healthcare-opengauss-dev
```

### 0.2 确认后端 `.env`

打开：

```powershell
notepad .\backend\.env
```

确认至少包含：

```env
DATABASE_URL=postgresql+psycopg2://healthcare:Healthcare%40123@localhost:5433/health_security
MEDICAL_RECORD_KEY=rvlc43/VxbvHAOMDYRZ7IyG1PqRDFvEo7hbClSmmqxc=
TZ=Asia/Shanghai
```
注意：.env中的healthcare只是D成员为了在后端连接数据库建的本地用户，其他人需要根据自己的修改
### 0.3 启动后端

新开 PowerShell：

```powershell
cd D:\Codex项目\医疗系统\Smart-Healthcare-Record-System\backend
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

看到下面内容表示正常：

```text
Application startup complete.
Uvicorn running on http://127.0.0.1:8000
```

### 0.4 启动前端

新开 PowerShell：

```powershell
cd D:\Codex项目\医疗系统\Smart-Healthcare-Record-System\frontend
npm.cmd run dev
```

打开：

```text
http://localhost:5173
```

## 1. 基础健康检查

### 1.1 后端健康接口

执行：

```powershell
Invoke-WebRequest -Uri http://127.0.0.1:8000/health
```

预期：

```text
status: ok
```

### 1.2 登录页加载

浏览器打开：

```text
http://localhost:5173
```

预期：

- 能看到登录页面。
- 没有白屏。
- 浏览器控制台没有明显 API 地址错误。

## 2. 演示账号测试

所有默认密码：

```text
password123
```

逐个测试登录：

| 账号 | 角色 | 预期页面 |
|---|---|---|
| `admin` | ADMIN | Admin Dashboard |
| `doctor1` | DOCTOR | Doctor Dashboard |
| `patient1` | PATIENT | Patient Dashboard |
| `auditor` | AUDITOR | Auditor Dashboard |

通过标准：

- 正确账号能登录。
- 点击 Logout 后能退出。
- 禁用账号不能登录。

## 3. 管理员功能测试

登录：

```text
admin / password123
```

### 3.1 系统状态和安全边界

进入 Admin Dashboard。

检查顶部状态：

- Users 有数量。
- Doctors 有数量。
- Patients 有数量。
- Disabled 有数量。

检查 Security Boundary：

- Medical Records 显示病历数量。
- Consents 显示授权数量。
- Recent Audit Logs 显示最近审计日志数量。
- Hash Chain 显示 Valid 或 Needs Check。
- 页面有管理员不能查看解密病历正文的提示。

通过标准：

- 管理员页面只显示统计，不提供查看病历正文按钮。

### 3.2 创建账号

进入 Accounts 标签页。

测试创建审计员：

```text
Role: AUDITOR
Username: auditor_test
Password: password123
```

预期：

- 创建成功。
- 用户列表出现 `auditor_test`。
- 退出后可用 `auditor_test / password123` 登录审计员页面。

测试创建医生：

```text
Role: DOCTOR
Username: doctor_test
Password: password123
Department: General Medicine
License No.: DOC-TEST-001
Note: test doctor
```

预期：

- 创建成功。
- Doctor Review 出现 `doctor_test`。
- 默认 Review 状态为 Pending。

### 3.3 禁用、启用、重置密码

在 Accounts 中选择 `auditor_test` 或 `doctor_test`。

测试 Disable：

- 点击 Disable。
- 退出后尝试登录该账号。
- 预期登录失败。

测试 Enable：

- 点击 Enable。
- 再次登录。
- 预期登录成功。

测试 Reset：

- 点击 Reset。
- 设置密码为：

```text
password123
```

- 使用新密码登录。
- 预期成功。

注意：不要禁用当前正在使用的 `admin` 账号。

### 3.4 医生审核

进入 Doctor Review。

选择 `doctor1`，点击 Edit。

设置：

```text
Department: General Medicine
License No.: DOC-001
Review: Approved
Account Status: Active
```

点击 Save。

预期：

- `doctor1` 状态为 APPROVED。
- `doctor1` 可以进入医生端并加载病人列表。

负向测试：

- 将 `doctor1` 的 Review 改回 Pending。
- 登录 `doctor1`。
- 预期医生端业务接口无法加载病人或搜索患者。
- 测试完改回 Approved。

### 3.5 医患分配

进入 Patient Assignment。

选择：

```text
Approved Doctor: doctor1
Patient: 任意患者
```

点击：

```text
Assign DEFAULT_CLINICAL
```

预期：

- 页面提示分配成功。
- `doctor1` 登录后，My Patients 中出现该患者。
- 授权范围为 Default Access。
- 这条默认授权有效期为 14 天。

## 4. 医生功能测试

登录：

```text
doctor1 / password123
```

前置条件：

- `doctor1` 已由管理员审核为 Approved。

### 4.1 我的病人列表

进入 My Patients。

检查分组：

- Full Access
- Default Access
- Pending Approval
- Rejected
- Revoked
- Expired Access

预期：

- 管理员分配的患者出现在 Default Access。
- 过期授权出现在 Expired Access。

### 4.2 默认授权查看病历

在 Default Access 分组点击 View Record。

预期：

- 能看到患者基础信息和诊断信息。
- 检查、用药、操作等更敏感内容应显示限制提示，或只展示默认范围内容。

### 4.3 申请 Full Access

在 Default Access 分组点击 Apply Full Access。

填写理由：

```text
Need complete record for treatment plan.
```

预期：

- 提交成功。
- 患者端出现待审批申请。
- 医生端该患者进入 Pending Approval 或状态显示待审批。

### 4.4 搜索患者

进入 Search Patients。

搜索患者姓名关键词。

预期：

- 能返回患者列表。
- 根据状态显示不同按钮：
  - No Access：可申请 Default 或 Full。
  - Default Access：只能申请 Full。
  - Full Access：不能重复申请。
  - Pending：显示等待。
  - Rejected/Expired：可重新申请。

## 5. 患者功能测试

登录：

```text
patient1 / password123
```

### 5.1 查看本人病历

进入 Patient Dashboard。

预期：

- 能看到患者基本信息。
- 能看到诊断、就诊、检查、用药、操作等统计或列表。
- 患者只能看自己的病历。

### 5.2 审批医生申请

进入 Doctor Authorization。

找到医生提交的待审批申请。

测试 Approve：

- 点击 Approve。
- 填写备注。
- 预期授权变为 ACTIVE。
- 授权有效期为 14 天。

测试 Reject：

- 对另一个申请点击 Reject。
- 预期医生端显示 Rejected，可重新申请。

### 5.3 撤销授权

在已授权医生列表中选择一条 ACTIVE 授权。

点击 Revoke。

预期：

- 授权状态变为 REVOKED。
- 医生端再次查看该患者病历会被拒绝。

## 6. 授权 14 天过期测试

不用真的等 14 天，手动把 `end_time` 改成过去时间。

进入数据库：

```powershell
docker exec -it healthcare-opengauss-dev bash
```

```bash
su - omm
gsql -d health_security
```

注意：当前后端使用 `healthcare` 数据库用户，授权数据在 `healthcare.consents`。如果是omm等会会有另外的说明。

查看 `doctor1` 的授权：

```sql
SELECT id, doctor_id, patient_id, status, record_scope, end_time
FROM healthcare.consents
WHERE doctor_id = 2
ORDER BY id;
```

把某条 ACTIVE 授权改成已过期：

```sql
UPDATE healthcare.consents
SET end_time = CURRENT_TIMESTAMP - interval '1 day'
WHERE id = 1;
```

确认已过期：

```sql
SELECT id, doctor_id, patient_id, status, record_scope, end_time
FROM healthcare.consents
WHERE id = 1;
```

刷新 `doctor1` 页面。

预期：

- 该患者出现在 Expired Access。
- 不能 View Record。
- 可以重新申请 Default 或 Full Access。

恢复测试数据：

```sql
UPDATE healthcare.consents
SET end_time = CURRENT_TIMESTAMP + interval '14 days'
WHERE id = 1;
```

退出：

```sql
\q
exit
exit
```

如果是omm:
```sql
SELECT id, doctor_id, patient_id, status, record_scope, end_time
FROM consents
WHERE doctor_id = 2
ORDER BY id;
```
其他操作类似，把表名改了就行

## 7. 病历加密测试

进入数据库：

```powershell
docker exec -it healthcare-opengauss-dev bash
su - omm
gsql -d health_security
```

查询密文：

```sql
SELECT id, left(encrypted_data, 80) AS encrypted_preview, nonce
FROM healthcare.medical_records
LIMIT 5;
```

预期：

- `encrypted_data` 不是可读病历正文。
- 数据库里不能直接看到诊断、检查、用药明文 JSON。

## 8. 审计员功能测试

登录：

```text
auditor / password123
```

进入 Auditor Dashboard。

### 8.1 审计统计

预期能看到：

- Total Logs
- Denied Events
- Latest Event
- Hash Chain
- Action Counts
- Recent Audit Logs

### 8.2 哈希链验证

点击：

```text
Verify Hash Chain
```

预期：

```text
Valid
```

### 8.3 篡改检测

进入数据库：

```powershell
docker exec -it healthcare-opengauss-dev bash
su - omm
gsql -d health_security
```

查看日志：

```sql
SELECT id, action, outcome, current_hash
FROM healthcare.audit_logs
ORDER BY id
LIMIT 5;
```

篡改第 1 条：

```sql
UPDATE healthcare.audit_logs
SET outcome = 'TAMPERED'
WHERE id = 1;
```

回到 Auditor Dashboard，点击 Verify Hash Chain。

预期：

```text
Audit chain broken at log #1: current_hash mismatch
```

恢复：

```sql
UPDATE healthcare.audit_logs
SET outcome = 'SUCCESS'
WHERE id = 1;
```

如果第 1 条原值不是 `SUCCESS`，请先查询原值再恢复，或者重新恢复数据库 dump。

## 9. 权限边界测试

### 9.1 患者不能访问医生接口

用 `patient1` 登录后，直接访问医生页面或医生接口。

预期：

- 无法进入医生业务页面，或接口返回 403。

### 9.2 医生不能访问管理员页面

用 `doctor1` 登录后访问：

```text
http://localhost:5173/admin
```

预期：

- 路由拦截或接口返回 403。

### 9.3 管理员不能查看解密病历

用 `admin` 登录。

预期：

- 管理员页面没有查看病历正文入口。
- 只能看到病历数量和系统状态。

## 10. 上传 GitHub 前检查

### 10.1 前端类型检查

```powershell
cd D:\Codex项目\医疗系统\Smart-Healthcare-Record-System\frontend
npx.cmd vue-tsc --noEmit --incremental false -p tsconfig.app.json
```

预期：无报错。

### 10.2 前端构建

```powershell
npx.cmd vite build
```

预期：构建成功。大 chunk warning 可以接受。

### 10.3 后端语法检查

```powershell
cd D:\Codex项目\医疗系统\Smart-Healthcare-Record-System\backend
.\.venv\Scripts\python.exe -B -c "import ast, pathlib; files=['app/api/admin.py','app/api/auditor.py','app/api/auth.py','app/api/doctor.py','app/api/patient_records.py','app/db/init_db.py','app/main.py']; [ast.parse(pathlib.Path(f).read_text(encoding='utf-8'), filename=f) for f in files]; print('syntax ok')"
```

预期：

```text
syntax ok
```

### 10.4 Git 检查

```powershell
cd D:\Codex项目\医疗系统\Smart-Healthcare-Record-System
git status --short
```

确认不要上传：

- `backend/.env`
- `backend/.venv`
- `frontend/node_modules`
- `database/health_security.copy.sql`
- `frontend/dist`

## 11. 最终通过标准

全部满足后可以上传：

- 四类角色都能登录。
- 管理员能创建账号、审核医生、分配患者。
- 未审核医生不能访问医生业务。
- 医生只能在授权有效且未过期时查看病历。
- 患者能审批、拒绝、撤销授权。
- 病历数据库中是密文。
- 审计日志会记录关键操作。
- 哈希链正常时 Valid，篡改后能检测异常。
- 管理员不能查看解密病历正文。
- 前端类型检查通过。
- 后端语法检查通过。
